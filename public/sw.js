// Placeholder only -- see public/sitemap.xml's comment for why this file
// must exist at build time (same Nitro static-asset-route-table timing
// issue). Overwritten in place by scripts/generate-sw.mjs (real workbox
// precache manifest) after every build. This no-op body only matters if a
// build ever completes without that postbuild step running.
